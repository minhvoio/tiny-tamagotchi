# Changelog

## 2026-04-18

- Initial project scaffold: repo init, README, and `.gitignore`
- Add constitution in `specs/`: `mission.md`, `tech-stack.md`, `roadmap.md`
- Add Phase 0 spec folder `specs/2026-04-18-phase-0-project-skeleton/` with `requirements.md`, `plan.md`, `validation.md`
- Bootstrap Next.js 16 + React 19 + Tailwind v4 app (App Router, `src/`, `@/*` alias); minimal landing page with centered "Tiny Tamagotchi" title
- Enable TypeScript `noUncheckedIndexedAccess` on top of `strict`
- Configure Prettier with `eslint-config-prettier` harmony; add `format` / `format:check` / `typecheck` / `test` scripts
- Wire up Vitest + React Testing Library + jsdom with one smoke test
- Scaffold `src/{components,game,hooks,styles}` and `public/sprites` with `.gitkeep` for future phases
- Phase 1 (Pet on Screen): inline-SVG `<Pet />` pixel creature with `role="img"` + `aria-label="Tiny tamagotchi, idling"`
- Phase 1: `src/styles/pet.module.css` defines the `bob` keyframe, applies `image-rendering: pixelated`, and disables motion under `prefers-reduced-motion: reduce`
- Phase 1: `<PetStage />` wrapper with pastel floor strip, mounted on `/` beneath the heading
- Phase 1: RTL tests — `<Pet />` renders with the idling aria-label and the `pet` CSS Module class (hash-tolerant regex); `<PetStage />` renders its children
- Phase 1: browser verification script `tests/browser/phase1_verify.py` via the `webapp-testing` skill — baseline screenshot, DOM assertions, bob-animation frame diff, reduced-motion regression, and console audit (all zero)
- Phase 2 (Hunger Vital): pure game core under `src/game/` — `constants.ts` (MAX_STAT=100, MIN_STAT=0, TICK_INTERVAL_MS=3000, DECAY_PER_TICK=1, FEED_AMOUNT=20), `state.ts` (Stat, Vitals, PetModel, Action union FEED|TICK co-located), `util.ts` (`clamp`), `reducer.ts` (pure, integer-only, clamped to [0,100], FEED and TICK with elapsedMs)
- Phase 2: `src/hooks/useTick.ts` (setInterval-driven TICK dispatch with cleanup); `src/hooks/useTamagotchi.tsx` — `"use client"` Provider owning `useReducer` + `useTick` plus a context hook that throws outside the provider
- Phase 2: `<StatBar />` — `role="progressbar"` with live `aria-valuenow`/`aria-valuemin`/`aria-valuemax`/`aria-label` plus a numeric `value / max` readout; `<FeedButton />` (`"use client"`) — dispatches FEED, disabled at MAX_STAT
- Phase 2: home page wraps its content in `<TamagotchiProvider>` and renders Hunger bar + Feed button beneath the pet stage
- Phase 2: vitest coverage — reducer, useTick (fake timers + cleanup), TamagotchiProvider/useTamagotchi (state updates + error outside provider), StatBar (ARIA + clamp), FeedButton (disabled at MAX_STAT, dispatch delta, re-disables on clamp) — 27 total tests passing
- Phase 2: browser verification `tests/browser/phase2_verify.py` via webapp-testing — initial state (100/100 + disabled Feed), decay over ~2 ticks, feed delta = FEED_AMOUNT, clamp boundary (spam Feed → stays at 100), long-idle floor (hunger reaches 0 and holds, no permadeath), console audit (zero errors/warnings/pageerrors)
- Phase 3 (Full Care Loop): extend `Vitals` to `{ hunger, happiness, energy }` and add `isResting: boolean` to `PetModel`; grow `Action` union with `PLAY` and `REST` (still co-located in `state.ts`, no `HEAL`/`RESET` yet)
- Phase 3: `src/game/constants.ts` adds `CARE_AMOUNTS` (`feed: { hunger: 20, happiness: 5 }`, `play: { happiness: 20, energy: -15 }`), `PLAY_MIN_ENERGY=10`, `REST_RECOVERY_PER_TICK=10`; removes the now-redundant `FEED_AMOUNT`
- Phase 3: reducer gains `FEED`/`PLAY`/`REST`/`TICK` branches with `isResting` gates (Feed/Play no-op while resting, Play also no-op below `PLAY_MIN_ENERGY`), per-tick vital decay while awake, energy recovery while resting, and **atomic auto-wake** — a single `TICK` that brings energy to 100 clears `isResting` in the same returned state object (no second dispatch)
- Phase 3: generic `<ActionButton />` with `aria-disabled` + `title={disabledReason}`; `<FeedButton />` refactored onto it; new `<PlayButton />` and `<RestButton />` (label toggles `Rest`/`Wake`, never disabled)
- Phase 3: home page renders three `<StatBar />`s in fixed order Hunger → Happiness → Energy and a row of Feed/Play/Rest buttons under the pet stage
- Phase 3: vitest coverage grows to 50 tests — full reducer matrix (FEED gated, PLAY gated + clamped, REST toggle, TICK awake/resting, atomic auto-wake assertion on one reducer output), `<ActionButton />` aria/title/click handling, `<PlayButton />`/`<RestButton />` gating and toggling, and a `tests/integration/care-loop.test.tsx` that runs feed → play → rest → auto-wake and Wake mid-recovery without vitals leaving [0, 100]
- Phase 3: browser verification `tests/browser/phase3_verify.py` via webapp-testing — bar ordering (Hunger/Happiness/Energy), button ordering (Feed/Play/Rest), care-loop sequence (decay, feed re-disables with "Pet is full", Play tires to "Too tired to play", Rest flips the label and freezes Hunger/Happiness), **atomic auto-wake** (polls `(buttonLabel, energy)` together across the transition and asserts no frame ever shows `Wake` + energy=100), keyboard Tab reachability of all three buttons, and zero console/page errors
- Phase 4 (Dynamic States & Heal): add `PetState = 'Normal' | 'Sick' | 'Evolved'`, `hasEvolved: boolean`, per-vital `neglectTicks`, scalar `careTicks`, and a `HEAL` action to `PetModel`/`Action`; introduce `SICK_VITAL_THRESHOLD=10`, `SICK_NEGLECT_TICKS=10`, `EVOLVE_VITAL_THRESHOLD=70`, `EVOLVE_CARE_TICKS=60`, `HEAL_SAFE_BAND=50` in `constants.ts`
- Phase 4: new `src/game/states.ts` — pure `nextState()` state machine called exclusively from the reducer TICK branch; Sick is only exited by HEAL, Evolved is a one-way latch guarded by `hasEvolved`
- Phase 4: reducer routes `state`/`hasEvolved` writes through `states.nextState()` everywhere except the HEAL branch (clamps vitals `<50` up to 50, returns to `hasEvolved ? 'Evolved' : 'Normal'`, resets counters) and the `__SEED__` test harness (exhaustive three-preset switch: `evolve-near`, `sick-near`, `evolved-near-sick`); rest pauses (does not reset) both counter sets
- Phase 4: `<Pet />` reads state via `useTamagotchi()` and sets `data-state` on the SVG with an `aria-label` of `Tiny tamagotchi, (idling|sick|thriving)`; `src/styles/pet.module.css` swaps `--pet-fill`/`--pet-eye`/`--pet-mouth`/`--pet-crown` per state; Evolved renders a decorative `<path data-testid="crown" aria-hidden="true">`
- Phase 4: `<PetStage />` renders a `<span data-testid="sick-indicator" aria-hidden="true">•••</span>` only while Sick; `<HealButton />` renders only while Sick (null otherwise — never a disabled button); `<StateAnnouncer />` is a `role="status" aria-live="polite"` div whose text becomes `"Pet is now {state}"` on every transition (silent on initial mount)
- Phase 4: `TamagotchiProvider` reads `?__seed=` from `window.location.search` exactly once on mount and dispatches `__SEED__` for recognized names; unknown seeds no-op; `page.tsx` adds `<HealButton />` after `<RestButton />` and mounts `<StateAnnouncer />` beneath the button row
- Phase 4: vitest coverage grows to 105 tests — `states.ts` purity and transition tests (no `Date.now`/`Math.random` calls), HEAL branches (identity outside Sick, clamp to 50, Evolved-aware recovery), atomic `Normal→Sick` / `Evolved→Sick` / `Normal→Evolved` on one reducer output, one-way `hasEvolved` across 500 mixed TICKs, forbidden transition assertions (`Evolved→Normal` via TICK never happens; `Sick→Normal` only via HEAL), scope-contract text grep (literal `state: 'X'` assignments are confined to `SEED_PRESETS`), seed harness (each preset + unknown + exactly-once on mount), Pet per-state rendering, `<HealButton />` lifecycle, `<StateAnnouncer />` announcements, and a `tests/integration/state-machine.test.tsx` that runs the full `Normal→Sick→HEAL→Normal` and `Evolved→Sick→HEAL→Evolved` loops end-to-end through the provider
- Phase 4: browser verification `tests/browser/phase4_verify.py` via webapp-testing — baseline Normal (no crown, no `•••`, no Heal), Sick transition via `?__seed=sick-near` (crown absent, indicator + Heal visible, announcer `Pet is now Sick`), HEAL atomicity (data-state flips to Normal and Heal button disappears in one waited frame, vitals all ≥50), Evolved via `?__seed=evolve-near` (crown present, `thriving` label), `Evolved→Sick→HEAL→Evolved` via `?__seed=evolved-near-sick` (crown returns, never Normal), another neglect/heal cycle that confirms `hasEvolved` is persistent, keyboard Tab reachability of Heal only when rendered, and zero console/page errors
- Phase 5 (Persistence, Naming & Real-Time Aging): extend `PetModel` with `name: string` and `lastTickAt: number`; add `MAX_OFFLINE_MS = 8 * 60 * 60 * 1000` to `constants.ts`; grow the `Action` union with `RESET`, `SET_NAME`, and an internal `__HYDRATE__`; reshape `TICK` to `{ elapsedMs, nowMs }` so the reducer never calls `Date.now()`
- Phase 5: add `src/game/storage.ts` — a pure adapter (`read`, `write`, `clear`, `isAvailable`) with single key `tiny-tamagotchi:v1` and schema `version: 1`; corrupt JSON and version mismatches return `null` without throwing so the app falls back to the naming form
- Phase 5: reducer gains `RESET` (returns `initialState`), `SET_NAME` (trim/validate-before-dispatch), and `__HYDRATE__` (returns `action.state` directly); every TICK branch sets `lastTickAt = action.nowMs` on the returned state
- Phase 5: `TamagotchiProvider` hydrates once on mount — `storage.read()` → `__HYDRATE__` → a single catch-up `TICK` with `elapsedMs = Math.max(0, Math.min(Date.now() - stored.lastTickAt, MAX_OFFLINE_MS))` — then a write-on-change `useEffect` calls `storage.write(state)` while `state.name !== ''` and `storage.clear()` when the name is empty; `useTick` now passes `nowMs: Date.now()` on every dispatch
- Phase 5: new `<NamingForm />` (heading `"Name your pet"`, input with `maxLength=24`, `"Confirm"` button, inline `"Name must be 1-24 characters"` error with `role="alert"` + `aria-describedby`) blocks the whole app until `state.name !== ''`; new `<ResetButton />` calls `window.confirm('Reset your pet? This cannot be undone.')` and dispatches `RESET` only on OK
- Phase 5: `src/app/page.tsx` splits into a `<PetScreen />` child that renders the naming form when name is empty and otherwise renders the Tiny Tamagotchi UI plus a small settings row (`{name}` + `<ResetButton />`)
- Phase 5: vitest coverage grows to 137 tests — reducer branches (`TICK` sets `lastTickAt`, `RESET` atomically restores `initialState`, `SET_NAME` sets name while preserving other fields, `__HYDRATE__` returns `action.state` directly), storage adapter (round-trip, version check, corrupt JSON, mismatch, jsdom `isAvailable`), `<NamingForm />` validation (empty + whitespace rejected, 1 and 24 char accepted, trimming dispatched), `<ResetButton />` confirm-OK/Cancel behavior, `tests/integration/persistence.test.tsx` (hydration, offline catch-up under and over `MAX_OFFLINE_MS`, write-on-change, full Reset flow, corrupt + version recovery)
- Phase 5: browser verification `tests/browser/phase5_verify.py` via webapp-testing — naming-form gate on clean load, empty-submit error, name acceptance, persistence across reload, offline catch-up (≈60s lowers Hunger), offline cap (10 days collapses to exactly `MAX_OFFLINE_MS` of decay ±1), native-confirm Reset flow leaves `localStorage` cleared, corrupt-JSON + version-mismatch recovery, and zero console/page errors
